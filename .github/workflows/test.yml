name: Test API
# When it's trigger
on:
  push:
    branches: master
  pull_request:
    branches: master
# Tasks
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    # To clone code to work on
    - name: Checkout repository
      run: ls
    